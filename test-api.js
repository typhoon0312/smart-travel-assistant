import { GoogleGenerativeAI } from "@google/generative-ai";
import fetch from "node-fetch";

// We need an API key to test! The user has it in LocalStorage, let me see if I can run this with a simple dummy key, wait no I can't.
